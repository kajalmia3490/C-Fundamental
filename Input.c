// Input means that data collect from users.
// For example:
#include <stdio.h>
int main()
{
    int first, second, add;
    scanf("Enter your first number: %d", &first);
    scanf("Enter your first number: %d", &second);
    add = first + second;
    printf("Total sum is: %d", add);

    return 0;
}