// Variable means that a like container. Whereas some data stored in variable.
// For example:
#include <stdio.h>
int main()
{
    int a = 20;
    int b = 30;
    int sum = a + b;
    printf("Your sumation is: %d", sum);

    return 0;
}