// Statement means by programming sentence or line. Every line or sentence is called statement.
// For example: 
#include <stdio.h>
int main ()
{
    long a, b, c, sum; // statement
    a = 3; // statement
    b = a; // statement
    c = a + b; // statement
    sum = a + b + c; // statement
    printf("Calculation is: %d", sum); // statement
    return 0; // statement
}